/**
 * Digital Frame - 前端配置文件
 * 所有可配置项集中管理
 */

// ============================================
// 应用配置
// ============================================
var APP_CONFIG = {
  // 版本号
  version: 'v0.1.0',

  // ============================================
  // 图片轮播配置
  // ============================================
  slideshow: {
    // 图片切换间隔（毫秒）
    interval: 8000,
    // 图片过渡动画时长（毫秒）
    transitionDuration: 1500,
    // 图片API地址（后端服务）
    apiUrl: '/api/photos',
    // 图片加载失败时的重试次数
    maxRetries: 3,
    // 预加载图片数量（前后各预加载几张）
    preloadCount: 2,
  },

  // ============================================
  // 天气配置
  // ============================================
  weather: {
    // 天气API地址（后端代理）
    apiUrl: '/api/weather',
    // 天气刷新间隔（毫秒）- 默认30分钟
    refreshInterval: 30 * 60 * 1000,
    // 温度单位: 'c' 摄氏 / 'f' 华氏
    unit: 'c',
  },

  // ============================================
  // 时钟配置
  // ============================================
  clock: {
    // 时钟刷新间隔（毫秒）
    refreshInterval: 1000,
    // 日期格式: 'zh-CN' 中文 / 'en' 英文
    locale: 'zh-CN',
  },
};

// 导出配置（全局可用）
window.APP_CONFIG = APP_CONFIG;
